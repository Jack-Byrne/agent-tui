import { runCoordinator } from "./agents/coordinator.js";

const input =
  process.argv.slice(2).join(" ") || "Calculate rough effort for two engineers for 3 weeks.";

runCoordinator(input)
  .then(console.log)
  .catch((err) => {
    console.error(err);
    process.exitCode = 1;
  });
